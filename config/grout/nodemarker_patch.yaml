apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodemarker
  namespace: system
spec:
  template:
    spec:
      containers:
        - name: nodemarker
          args:
          - "--loglevel=debug"
          - "--namespace=$(NAMESPACE)"
          - "--webhookmode=enabled"
          - "--datapath=grout"
